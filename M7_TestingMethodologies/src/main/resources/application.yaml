spring:
  application:
    name: M7_TestingMethodologies

  datasource:
    url: jdbc:mysql://localhost:3306/SpringBoot_Test
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: create

    show-sql: true

    properties:
      hibernate:
        format_sql: true